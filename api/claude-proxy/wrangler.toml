name = "tarot-claude-proxy"
main = "index.js"
compatibility_date = "2024-01-01"

# 环境变量（在 Cloudflare Dashboard 中设置）
# GEMINI_API_KEY - 你的 Google Gemini API Key（通过 wrangler secret put GEMINI_API_KEY 设置）
[vars]
# CLOUDFLARE_WORKER_URL 将在部署后自动生成
